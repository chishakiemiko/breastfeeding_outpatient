<% flash.each do |key, value| %>
  <div class="alert alert-<%= 'warning' if key == 'notice' %><%= 'danger' if key == 'alert' %> vw-100 text-center" role="alert" >
    <p><%= value %></p>
  </div>
<% end %>